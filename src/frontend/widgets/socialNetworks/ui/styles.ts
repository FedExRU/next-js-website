import { createSxStyles } from '../../../shared/lib'

export const getSocialNetworksStyles = () =>
  createSxStyles({
    flexWrapper: {
      alignItems: 'center',
      display: 'flex',
    },
  })
